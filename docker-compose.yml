services:
  extractor:
    build: .
    restart: unless-stopped
    environment:
      - MONGO_CONNECTION=${MONGO_CONNECTION:-}
    volumes:
      - ./scoreboards_out:/app/scoreboards_out
      - ./videos_out:/app/videos_out
