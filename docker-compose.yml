services:

  # ── Automatic stream extractor ───────────────────────────────────────────────────
  stream:
    build:
      context: ./stream
    restart: unless-stopped
    environment:
      - MONGO_URI=${MONGO_URI:-}
    volumes:
      - ./stream/output:/app/output

  # ── Manual web dashboard ─────────────────────────────────────────────────────────
  manual:
    build:
      context: .
      dockerfile: manual_web/Dockerfile
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      - TBA_KEY=${TBA_KEY:-}
      - MONGO_URI=${MONGO_URI:-}
      - UPLOAD_TO_MONGO=${UPLOAD_TO_MONGO:-false}
